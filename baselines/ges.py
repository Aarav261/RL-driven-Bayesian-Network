"""Required baseline 2: score-equivalent Greedy Equivalence Search (GES).

Wrap an existing implementation (pgmpy / causal-learn) rather than reimplement.
"""


def ges(data, cfg):
    """Run GES on `data`; return a DAG from the learned CPDAG."""
    raise NotImplementedError
